{
  "name": "shift-log",
  "private": true,
  "version": "0.1.0",
  "description": "ShiftLog — permission-based activity memory for cloud agents (Computer History compatible)",
  "packageManager": "pnpm@10.33.3",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r --parallel run dev",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
