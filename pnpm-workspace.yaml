packages:
  - "apps/*"
  - "services/*"
  - "packages/*"

# Opt out of pnpm 11's 24h minimumReleaseAge so renovate-bumped lockfile entries install in CI.
minimumReleaseAge: 0

# tsx / vitest / vite depend on esbuild's postinstall binary.
allowBuilds:
  esbuild: true
